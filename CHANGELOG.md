# Changelog

## 0.3.3 - 2026-08-24

- 原生菜单桥接只拦截 FrameClip 使用的 `ID_APP_ABOUT` 占位命令；其他 PotPlayer 子菜单命令会继续交还播放器处理，不再被错误映射为 FrameClip 动作。
- 卸载器会验证安装状态中的 PotPlayer 目录、菜单路径、配置路径和菜单 XML 身份，阻止被篡改的用户态状态文件触发任意文件删除或设置修改。
- FFmpeg 与 FFprobe 的标准输出、错误输出改为并发排空，并按操作类型设置超时；大量诊断输出不再造成重定向管道死锁。
- 新增高容量标准错误压力测试、恶意卸载状态回归测试，以及菜单命令白名单静态门槛。

## 0.3.2 - 2026-08-24

- 移除独立悬浮操作菜单，九项功能直接在 PotPlayer 原生子菜单中分发。
- 移除会展开“配置/语言/其他”三级菜单的 `CMD_POPUPMENU_ETC` 占位命令，改用无子菜单、非播放类的叶子命令作为失效后备。
- 移除皮肤菜单入口的尺寸、DPI、首行位置和相邻子菜单推断；这些推断会把普通 PotPlayer 子菜单误认为 FrameClip。
- 移除 Windows UI Automation 菜单识别；PotPlayer 自绘菜单 Provider 会阻塞树扫描和点查询，导致原始“关于”命令继续执行。
- 新增 x64/x86 原生菜单桥接。线程级 Hook 将匹配位数的独立 DLL 装入 PotPlayer，窗口子类在进程内拦截待处理的 FrameClip `WM_COMMAND`，不修改 PotPlayer 自带文件。
- 新增 32 位桥接宿主，64 位主程序可同时支持 32 位 PotPlayer。
- 桥接 DLL 在 PotPlayer 进程生命周期内固定驻留，防止 Hook 所有者退出后窗口子类指向已卸载代码；安装与升级会要求先关闭播放器。
- 原生桥接枚举并精确匹配隐藏消息窗口，把动作发送给常驻 FrameClip 进程；不再依赖在部分系统上会漏检窗口的标题查询捷径。
- 右下角提示改为单窗体双缓冲绘制，移除两个可能显示为纯黑矩形的 `Label` 子窗口；回退进程也会保持消息循环直至提示关闭。
- 普通菜单或未匹配点击立即结束本次桥接会话，不再延长全局鼠标状态。
- 点击 FrameClip 菜单之外的任意区域不会被拦截，也不会触发 FrameClip 功能。
- 新增静态门槛，禁止重新引入 UI Automation、桌面级菜单扫描和独立悬浮操作菜单。
- 修复安装器初始化时使用无效 `{userprofile}` 常量而直接退出的问题。

## 0.3.1 - 2026-08-21

- 重构 PotPlayer 皮肤菜单交互：点击 FrameClip 入口后显示由 FrameClip 自身拥有的操作菜单，不再依赖九个自绘菜单行的坐标猜测。
- FrameClip XML 子菜单彻底移除 `ID_PLAY_PAUSE` 占位命令；桥接程序未运行或识别失败时也不会改变播放状态。
- 优先使用 PotPlayer 实际菜单窗口矩形和窗口标题，仅将 DPI 几何作为入口识别后备。
- 新增发布门槛和安装回归测试，禁止 FrameClip 命令再次绑定播放/暂停。

## 0.3.0 - 2026-08-21

- 新增可选的 HDR 截帧 Rec.709 SDR 副本；PQ/HLG 原图照常保留，副本使用线性浮点处理与 Mobius 色调映射，默认关闭。
- 新增简体中文与英语界面切换，覆盖设置窗口、原生菜单桥接菜单和 PotPlayer XML 右键菜单。
- 切换语言时安全更新已安装菜单；Program Files 等受保护目录会请求一次 Windows 管理员确认。
- 安装升级会保留既有语言选择，并记录实际 PotPlayer 菜单路径。
- 增加双语菜单迁移、用户自定义菜单保留、HDR 色调映射滤镜和安装默认值回归测试。

## 0.2.0 - 2026-08-20

- 新增单文件 Inno Setup 安装包；GitHub Release 仅发布版本化的 `Setup.exe`。
- 修复 PotPlayer `[RememberFiles]` 中“播放位置=完整路径”的解析，并加入进程命令行与可信同目录恢复。
- 接通自绘菜单标题与窗口缓存分发，降低菜单错位、外部窗口误触和按钮落回播放/暂停的概率。
- 安装期间不强制关闭 PotPlayer；播放器退出后由常驻程序完成菜单选择回写。
- 修复标准安装、便携版、旧产品名迁移、重复安装和卸载恢复。
- 修复纯数字集名与技术规格文件名被误判为电影标题；无法可靠识别时进入稳定的“待归类”目录。
- 修复连续提示关闭时的空引用竞态，以及自定义程序目录与用户数据目录失联。
- 保留 UTF-16、UTF-8 BOM、UTF-8 无 BOM 与系统 ANSI PotPlayer INI 的原编码。
- 新增 PNG/TIFF、ProRes/DNxHR 设置说明和色彩处理边界；修正 ProRes 4444 输入位深标注。
- 增加 C# 单元测试、菜单静态检查，以及隔离安装、重复安装、卸载回归测试。
- 根 README 改为完整中文发布文档，并补充静默安装、Agent 单命令安装、依赖与问题排查。

## 0.1.1 - 2026-08-14

- 将中文菜单名称调整为 `参照帧与片段截取`。
- 升级时清理旧版较短的菜单名称。

## 0.1.0 - 2026-08-14

- 应用更名为 PotPlayer FrameClip，移除特定后期软件名称。
- 新增用户级配置和旧版数据迁移。
- 支持 32 位、64 位与便携版 PotPlayer。
- 新增 FFmpeg 自动查找、菜单合并、INI 备份、卸载恢复、构建与 GitHub Actions。
